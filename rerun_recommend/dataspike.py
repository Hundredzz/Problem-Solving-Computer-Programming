"""code"""
def check(text):
    """function"""
    a = int(input())
    if a > text:
        text = a
    return text
def main():
    """function"""
    text = 0
    text = check(text)
    text = check(text)
    text = check(text)
    text = check(text)
    text = check(text)
    text = check(text)
    text = check(text)
    text = check(text)
    print(text)
main()
